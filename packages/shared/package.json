{
  "name": "@shared/utils",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run --passWithNoTests --exclude src/testing/integration.infrastructure.integration.test.ts",
    "test:integration": "vitest run src/testing/integration.infrastructure.integration.test.ts",
    "test:e2e": "vitest run --passWithNoTests",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "zod": "^4.4.3"
  }
}
